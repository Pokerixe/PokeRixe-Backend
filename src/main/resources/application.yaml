spring:
  application:
    name: pokerixe-backend
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/pokerixe}cat src/main/resources/application.yml

jwt:
  secret: ${JWT_SECRET:ma_cle_secrete_dev_de_32_chars_min}
  expiration: 86400000

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator